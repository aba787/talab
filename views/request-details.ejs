
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تفاصيل الطلب</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card shadow">
                    <div class="card-header bg-primary text-white">
                        <h2 class="text-center">تفاصيل الطلب</h2>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <h5>رقم الطلب</h5>
                                <p class="text-primary fw-bold"><%= request.request_id %></p>
                            </div>
                            <div class="col-md-6">
                                <h5>الحالة الحالية</h5>
                                <span class="badge 
                                    <% if (request.status === 'قيد المراجعة') { %>bg-warning<% } 
                                    else if (request.status === 'تحت المعالجة') { %>bg-info<% } 
                                    else if (request.status === 'منجز') { %>bg-success<% } 
                                    else { %>bg-secondary<% } %> 
                                    fs-6"><%= request.status %></span>
                            </div>
                        </div>
                        
                        <hr>
                        
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <h6>اسم مقدم الطلب</h6>
                                <p><%= request.name %></p>
                            </div>
                            <div class="col-md-6 mb-3">
                                <h6>البريد الإلكتروني</h6>
                                <p><%= request.email %></p>
                            </div>
                        </div>
                        
                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <h6>القسم/الإدارة</h6>
                                <p><%= request.department %></p>
                            </div>
                            <div class="col-md-6 mb-3">
                                <h6>نوع الطلب</h6>
                                <p><%= request.request_type %></p>
                            </div>
                        </div>
                        
                        <div class="mb-3">
                            <h6>تفاصيل الطلب</h6>
                            <p class="border p-3 bg-light"><%= request.description %></p>
                        </div>
                        
                        <% if (request.notes) { %>
                        <div class="mb-3">
                            <h6>ملاحظات الإدارة</h6>
                            <div class="alert alert-info">
                                <%= request.notes %>
                            </div>
                        </div>
                        <% } %>
                        
                        <div class="row">
                            <div class="col-md-6">
                                <small class="text-muted">تاريخ التقديم: <%= new Date(request.created_at).toLocaleDateString('ar-SA') %></small>
                            </div>
                            <div class="col-md-6">
                                <small class="text-muted">آخر تحديث: <%= new Date(request.updated_at).toLocaleDateString('ar-SA') %></small>
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="text-center mt-3">
                    <a href="/track" class="btn btn-outline-info">تتبع طلب آخر</a>
                    <a href="/" class="btn btn-outline-primary">العودة للرئيسية</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
