
<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تتبع الطلب</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card shadow">
                    <div class="card-header bg-info text-white text-center">
                        <h2>تتبع الطلب</h2>
                    </div>
                    <div class="card-body">
                        <% if (typeof error !== 'undefined') { %>
                            <div class="alert alert-danger">
                                <%= error %>
                            </div>
                        <% } %>
                        
                        <form action="/track" method="POST">
                            <div class="mb-3">
                                <label for="request_id" class="form-label">رقم الطلب</label>
                                <input type="text" class="form-control" id="request_id" name="request_id" 
                                       placeholder="أدخل رقم الطلب (مثال: REQ1234567890)" required>
                                <small class="form-text text-muted">
                                    يمكنك إدخال رقم الطلب يدوياً أو استخدام الماسح الضوئي للباركود
                                </small>
                            </div>
                            
                            <div class="text-center">
                                <button type="submit" class="btn btn-info btn-lg">تتبع الطلب</button>
                            </div>
                        </form>
                    </div>
                </div>
                
                <div class="text-center mt-3">
                    <a href="/" class="btn btn-outline-primary">العودة للرئيسية</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
